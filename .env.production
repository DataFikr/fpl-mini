NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# FPL API Configuration
FPL_API_BASE_URL=https://fantasy.premierleague.com/api

# Database - will be overridden by Vercel environment variables
# DATABASE_URL= (must be set in Vercel environment variables)

# Redis - will be overridden by Vercel environment variables if needed
# REDIS_URL= (leave empty to use in-memory cache)

# Disable worker threads completely
NODE_OPTIONS=--max-old-space-size=4096